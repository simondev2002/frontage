# Keys for the design lab only (never used by the server). Copy to .env in this folder.
OPENAI_API_KEY=sk-...        # platform.openai.com > API keys; model gpt-5.6-sol
MOONSHOT_API_KEY=sk-...      # platform.moonshot.ai > API keys; model kimi-k3
# ANTHROPIC_API_KEY is read from server/.env automatically
